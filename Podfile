# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NativeAdTestApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NativeAdTestApp
  pod 'Google-Mobile-Ads-SDK'

  target 'NativeAdTestAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NativeAdTestAppUITests' do
    # Pods for testing
  end

end
